apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timescaledb-pv-claim
  labels:
    app: timescaledb
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi